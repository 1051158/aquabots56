import request
import settings
import xls
import draw
import numpy as np
import triliteration

    wbk, wks, Settings, fig, ax, plt, coordinates, anchor1, anchor2, anchor3, anchor4, d1, d2, d3, d4 = settings.allSettings(Coordinates)
def SetDelays():

